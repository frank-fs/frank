# Work Packages: CLI Statechart Commands

**Inputs**: Design documents from `/kitty-specs/026-cli-statechart-commands/`
**Prerequisites**: plan.md (required), spec.md (user stories)

**Tests**: Not explicitly requested. Build verification only.

**Organization**: Fine-grained subtasks (`Txxx`) roll up into work packages (`WPxx`). Each work package must be independently deliverable and testable.

**Prompt Files**: Each work package references a matching prompt file in `/tasks/` generated by `/spec-kitty.tasks`. Treat this file as the high-level checklist; keep deep implementation detail inside the prompt files.

## Subtask Format: `[Txxx] [P?] Description`
- **[P]** indicates the subtask can proceed in parallel (different files/components).
- Include precise file paths or modules.

## Path Conventions
- **Library source**: `src/Frank.Statecharts/`, `src/Frank.Cli.Core/`, `src/Frank.Cli/`
- **Tests**: `test/Frank.Cli.Statechart.Tests/`

---

## Work Package WP01: XState Serializer & Project Visibility (Priority: P0)

**Goal**: Create the XState v5 JSON serializer/deserializer in `Frank.Statecharts/XState/` and add `InternalsVisibleTo` for `Frank.Cli.Core`. Add project reference from `Frank.Cli.Core` to `Frank.Statecharts`.
**Independent Test**: XState Serializer and Deserializer modules compile. Frank.Cli.Core can reference internal Frank.Statecharts modules.
**Prompt**: `/tasks/WP01-xstate-serializer-visibility.md`
**Estimated Size**: ~400 lines

### Included Subtasks
- [x] T001 Add `InternalsVisibleTo Include="Frank.Cli.Core"` to `Frank.Statecharts.fsproj`
- [x] T002 Add `Frank.Statecharts` project reference to `Frank.Cli.Core.fsproj`
- [x] T003 [P] Create `src/Frank.Statecharts/XState/Serializer.fs` — `StatechartDocument -> XState v5 JSON`
- [x] T004 [P] Create `src/Frank.Statecharts/XState/Deserializer.fs` — `XState v5 JSON -> Ast.ParseResult`
- [x] T005 Add XState compile entries to `Frank.Statecharts.fsproj` (before `StatefulResourceBuilder.fs`)
- [x] T006 Verify solution builds with `dotnet build`

### Implementation Notes
- XState v5 JSON schema: `{ id, initial, states: { [name]: { on: { [event]: target }, meta: {} } } }`
- Serializer maps `StatechartDocument` states/transitions to XState v5 nested format
- Deserializer reads XState v5 JSON and produces `Ast.ParseResult` (best-effort with errors)
- Follow existing pattern from other format modules (ALPS, SCXML, smcat)

### Parallel Opportunities
- T003 and T004 can proceed in parallel after T001+T002+T005 are done.

### Dependencies
- None (starting package).

### Risks & Mitigations
- XState v5 schema complexity: implement flat-state mapping only (no parallel/compound states).

---

## Work Package WP02: StatechartExtractor — Assembly Loading & Metadata Extraction (Priority: P0)

**Goal**: Create the `StatechartExtractor` module that loads a compiled .NET assembly and extracts `StateMachineMetadata` instances from endpoint metadata.
**Independent Test**: Module compiles. Can load a test assembly and extract metadata (if a test assembly is available).
**Prompt**: `/tasks/WP02-statechart-extractor.md`
**Estimated Size**: ~450 lines

### Included Subtasks
- [x] T007 Create `src/Frank.Cli.Core/Statechart/StatechartExtractor.fs` with `ExtractedStatechart` type and `extract` function
- [x] T008 Implement `AssemblyLoadContext` isolation with `isCollectible: true`
- [x] T009 Implement endpoint scanning: build minimal WebApplication, register endpoints, collect `StateMachineMetadata` from endpoint metadata
- [x] T010 Implement route template extraction from `RouteEndpoint.RoutePattern.RawText`
- [x] T011 Implement error handling: assembly not found, load failures, startup exceptions
- [x] T012 Add `StatechartExtractor.fs` compile entry to `Frank.Cli.Core.fsproj`
- [x] T013 Verify module compiles with `dotnet build`

### Implementation Notes
- Use host-based approach (D-003): load the assembly into an isolated `AssemblyLoadContext`, build a minimal `WebApplication`, register endpoints (which triggers `StatefulResourceBuilder.Run` and populates `StateMachineMetadata` on endpoint metadata), collect `StateMachineMetadata` instances from endpoint metadata, then shut down the host
- `AssemblyDependencyResolver` resolves the target assembly's `.deps.json`
- Return `Result<ExtractedStatechart list, string>` with clear error messages
- `ExtractedStatechart` contains: route template, state names, initial state, guard names, per-state metadata, raw `StateMachineMetadata`

### Parallel Opportunities
- Can proceed in parallel with WP01 if project references are set up first.

### Dependencies
- Depends on WP01 (needs `InternalsVisibleTo` and project reference).

### Risks & Mitigations
- Type identity mismatch risk (High): shared project reference ensures same assembly. If mismatch occurs, error message should explain.

---

## Work Package WP03: FormatDetector & FormatPipeline (Priority: P1)

**Goal**: Create the `FormatDetector` module (file extension -> format tag mapping) and the `FormatPipeline` module (metadata -> format text generation pipelines for all 5 formats).
**Independent Test**: Both modules compile. FormatDetector correctly maps known extensions. FormatPipeline produces output for each format.
**Prompt**: `/tasks/WP03-format-detector-pipeline.md`
**Estimated Size**: ~450 lines

### Included Subtasks
- [x] T014 [P] Create `src/Frank.Cli.Core/Statechart/FormatDetector.fs` with `detect` function and `FormatTag` -> extension mappings
- [x] T015 [P] Create `src/Frank.Cli.Core/Statechart/FormatPipeline.fs` with per-format generation pipelines
- [x] T016 Implement format detection: `.wsd`, `.alps.json`, `.scxml`, `.smcat`, `.xstate.json`, `.json` (ambiguous)
- [x] T017 Implement generation pipelines: WSD, ALPS, SCXML, smcat, XState (using existing generators + new XState serializer)
- [x] T018 Add compile entries to `Frank.Cli.Core.fsproj` (before command files)
- [x] T019 Verify modules compile with `dotnet build`

### Implementation Notes
- FormatDetector: compound extensions (`.alps.json`, `.xstate.json`) must be checked before `.json`
- FormatPipeline delegates to existing generators (all consume StatechartDocument directly, no mappers): `Wsd.Generator.generate`, `Wsd.Serializer.serialize`, `Alps.JsonGenerator.generateAlpsJson`, `Scxml.Generator.generate`, `Smcat.Generator.generate` + `Smcat.Serializer.serialize`, `XState.Serializer.serialize`
- Each pipeline takes `StateMachineMetadata` and returns `string` (the generated text)
- Resource slug derivation: extract from route template (e.g., `/games/{id}` -> `games`)

### Parallel Opportunities
- T014 and T015 can proceed in parallel.

### Dependencies
- Depends on WP01 (XState serializer, `InternalsVisibleTo`).
- Depends on WP02 (ExtractedStatechart type).

### Risks & Mitigations
- Internal module visibility: WP01 handles `InternalsVisibleTo`.

---

## Work Package WP04: StatechartDocumentJson & ValidationReportFormatter (Priority: P1)

**Goal**: Create the `StatechartDocumentJson` module (StatechartDocument -> JSON serialization for parse/extract output) and `ValidationReportFormatter` module (ValidationReport -> text/json output).
**Independent Test**: Both modules compile and produce correct JSON/text output from test data.
**Prompt**: `/tasks/WP04-document-json-report-formatter.md`
**Estimated Size**: ~400 lines

### Included Subtasks
- [x] T020 [P] Create `src/Frank.Cli.Core/Statechart/StatechartDocumentJson.fs` — serialize `StatechartDocument` to JSON
- [x] T021 [P] Create `src/Frank.Cli.Core/Statechart/ValidationReportFormatter.fs` — format `ValidationReport` as text and JSON
- [x] T022 Implement `StatechartDocumentJson.serialize`: states array, transitions array, initial state, data entries
- [x] T023 Implement `ValidationReportFormatter.formatText` and `formatJson`: passed/failed/skipped checks, failure diagnostics
- [x] T024 Add compile entries to `Frank.Cli.Core.fsproj`
- [x] T025 Verify modules compile with `dotnet build`

### Implementation Notes
- `StatechartDocumentJson` follows the `Utf8JsonWriter` pattern from existing `JsonOutput.fs`
- Include parse errors/warnings in the JSON when present (for parse command)
- `ValidationReportFormatter.formatText` uses color helpers from `TextOutput.fs` (respect `NO_COLOR`)
- `ValidationReportFormatter.formatJson` follows `JsonOutput.fs` conventions

### Parallel Opportunities
- T020 and T021 can proceed in parallel.

### Dependencies
- Depends on WP01 (Frank.Statecharts types visibility).

### Risks & Mitigations
- None significant. Pure serialization code.

---

## Work Package WP05: Extract Command (Priority: P1) — MVP

**Goal**: Implement the `frank statechart extract <assembly>` command logic in `StatechartExtractCommand.fs`.
**Independent Test**: Command module compiles. Given a loaded assembly with stateful resources, produces structured output (text and JSON).
**Prompt**: `/tasks/WP05-extract-command.md`
**Estimated Size**: ~350 lines

### Included Subtasks
- [ ] T026 Create `src/Frank.Cli.Core/Commands/StatechartExtractCommand.fs` with `execute` function
- [ ] T027 Implement text output: state names, initial state, guard names, per-state metadata (methods, final flag)
- [ ] T028 Implement JSON output: array of state machine entries with all metadata fields
- [ ] T029 Handle zero-stateful-resources case (message, exit code 0)
- [ ] T030 Handle assembly load errors (non-zero exit code, clear message)
- [ ] T031 Add compile entry to `Frank.Cli.Core.fsproj`

### Implementation Notes
- Calls `StatechartExtractor.extract` for assembly loading
- Text output format: table-like display per resource (route, states, initial, guards, methods)
- JSON output: `{ status: "ok", stateMachines: [ { routeTemplate, states, initialState, guards, stateMetadata } ] }`
- Add output formatters to `JsonOutput.fs` and `TextOutput.fs`

### Parallel Opportunities
- None within this WP.

### Dependencies
- Depends on WP02 (StatechartExtractor).

### Risks & Mitigations
- Assembly loading edge cases handled in WP02.

---

## Work Package WP06: Generate Command (Priority: P1) — MVP

**Goal**: Implement the `frank statechart generate --format <format> <assembly>` command logic in `StatechartGenerateCommand.fs`.
**Independent Test**: Command module compiles. Given extracted metadata, generates output in each supported format.
**Prompt**: `/tasks/WP06-generate-command.md`
**Estimated Size**: ~400 lines

### Included Subtasks
- [ ] T032 Create `src/Frank.Cli.Core/Commands/StatechartGenerateCommand.fs` with `execute` function
- [ ] T033 Implement single-format generation: delegate to FormatPipeline per format
- [ ] T034 Implement `--format all` generation: iterate all 5 formats
- [ ] T035 Implement `--output <directory>` file writing with naming convention `{resourceSlug}.{extension}`
- [ ] T036 Implement `--resource <name>` filter for single-resource generation
- [ ] T037 Implement stdout output with resource and format headers when `--output` is not specified
- [ ] T038 Handle zero-resources and directory creation
- [ ] T039 Add compile entry to `Frank.Cli.Core.fsproj`

### Implementation Notes
- Format option values: `wsd`, `alps`, `scxml`, `smcat`, `xstate`, `all`
- File extensions: `.wsd`, `.alps.json`, `.scxml`, `.smcat`, `.xstate.json`
- Auto-create output directory if `--output` specified and directory doesn't exist
- `--output-format` (text/json) for command's own status messages, distinct from `--format` (notation format)

### Parallel Opportunities
- None within this WP.

### Dependencies
- Depends on WP02 (StatechartExtractor), WP03 (FormatPipeline).

### Risks & Mitigations
- Output format disambiguation (D-007): use `--format` for notation, `--output-format` for text/json status.

---

## Work Package WP07: Validate Command (Priority: P1)

**Goal**: Implement the `frank statechart validate <spec-file> [<spec-file>...] <assembly>` command logic in `StatechartValidateCommand.fs`.
**Independent Test**: Command module compiles. Given spec files and extracted metadata, produces validation report.
**Prompt**: `/tasks/WP07-validate-command.md`
**Estimated Size**: ~400 lines

### Included Subtasks
- [ ] T040 Create `src/Frank.Cli.Core/Commands/StatechartValidateCommand.fs` with `execute` function
- [ ] T041 Implement spec file parsing: detect format, read file, dispatch to parser
- [ ] T042 Implement code-truth artifact generation: `Wsd.Generator.generate` -> `FormatArtifact(Wsd)`
- [ ] T043 Implement validation orchestration: `Validator.validate` with self-consistency + cross-format rules
- [ ] T044 Implement text output: passed checks, failed checks with diagnostics, skipped checks
- [ ] T045 Implement JSON output using `ValidationReportFormatter.formatJson`
- [ ] T046 Handle unsupported format extension, file read errors, non-zero exit on failures
- [ ] T047 Add compile entry to `Frank.Cli.Core.fsproj`

### Implementation Notes
- Parse each spec file into a `FormatArtifact` (parsed document + format tag)
- Generate code-truth artifact from extracted metadata (D-006: WSD format)
- Pass all artifacts to `Validator.validate` with `SelfConsistencyRules.rules @ CrossFormatRules.rules`
- Non-zero exit code when `report.TotalFailures > 0`

### Parallel Opportunities
- None within this WP.

### Dependencies
- Depends on WP02 (StatechartExtractor), WP03 (FormatDetector + FormatPipeline), WP04 (ValidationReportFormatter).

### Risks & Mitigations
- Parser dispatch requires all format parsers to be accessible (via `InternalsVisibleTo`).

---

## Work Package WP08: Parse Command (Priority: P2)

**Goal**: Implement the `frank statechart parse <spec-file>` command logic in `StatechartParseCommand.fs`.
**Independent Test**: Command module compiles. Given a spec file in any supported format, produces JSON StatechartDocument output.
**Prompt**: `/tasks/WP08-parse-command.md`
**Estimated Size**: ~350 lines

### Included Subtasks
- [ ] T048 Create `src/Frank.Cli.Core/Commands/StatechartParseCommand.fs` with `execute` function
- [ ] T049 Implement format detection from file extension (reuse FormatDetector)
- [ ] T050 Implement parser dispatch: WSD, ALPS, SCXML, smcat, XState
- [ ] T051 Implement ambiguous `.json` handling: try both ALPS and XState, or require `--format`
- [ ] T052 Implement JSON output of parsed `StatechartDocument` (using `StatechartDocumentJson.serialize`)
- [ ] T053 Include parse errors/warnings in output, non-zero exit on errors
- [ ] T054 Add compile entry to `Frank.Cli.Core.fsproj`

### Implementation Notes
- Parser dispatch mirrors the parse pipeline in plan.md. All parsers return Ast.ParseResult directly (no mapper step).
- All parsers return `Ast.ParseResult` directly (no mapper step):
  - WSD: `Wsd.Parser.parseWsd`, ALPS: `Alps.JsonParser.parseAlpsJson`, SCXML: `Scxml.Parser.parseString`, smcat: `Smcat.Parser.parseSmcat`, XState: `XState.Deserializer.deserialize`
- WSD: `Wsd.Parser.parseWsd` (produces `Ast.ParseResult` directly)
- XState: `XState.Deserializer.deserialize` (produces `Ast.ParseResult` directly)

### Parallel Opportunities
- None within this WP.

### Dependencies
- Depends on WP01 (XState deserializer), WP03 (FormatDetector), WP04 (StatechartDocumentJson).

### Risks & Mitigations
- Ambiguous `.json` extension: handle gracefully with clear error message.

---

## Work Package WP09: CLI Wiring & Help Content (Priority: P1)

**Goal**: Wire all 4 statechart subcommands into `Program.fs` under a `statechart` parent command. Register help metadata for each new command.
**Independent Test**: `frank-cli semantic extract`, `statechart extract`, `statechart generate`, `statechart validate`, `statechart parse` all accessible. `frank-cli help statechart extract` and `frank-cli help semantic extract` return distinct help.
**Prompt**: `/tasks/WP09-cli-wiring-help.md`
**Estimated Size**: ~500 lines

### Included Subtasks
- [ ] T055 Add `statechart` parent Command to `Program.fs` root command
- [ ] T056 Wire `extract` subcommand with `<assembly>` argument and `--output-format` option
- [ ] T057 Wire `generate` subcommand with `--format`, `<assembly>`, `--output`, `--resource`, `--output-format` options
- [ ] T058 Wire `validate` subcommand with `<spec-file>` arguments, `<assembly>` argument, `--output-format` option
- [ ] T059 Wire `parse` subcommand with `<spec-file>` argument, `--format` option (for notation disambiguation), `--output-format` option
- [ ] T060 Add statechart command help entries to `HelpContent.fs`
- [ ] T061 Verify all commands are accessible via CLI and existing commands are unaffected

### Implementation Notes
- Follow existing imperative System.CommandLine pattern in Program.fs (D-001)
- `statechart` parent command with Description: "Statechart pipeline operations for stateful Frank resources"
- Each subcommand follows the existing pattern: parse args, call command module, format output, handle errors
- Help entries follow spec 016 patterns with summary, examples, workflow, context

### Parallel Opportunities
- T060 can proceed in parallel with T055-T059 once help types are available.

### Dependencies
- Depends on WP05, WP06, WP07, WP08 (all command modules).

### Risks & Mitigations
- Regression risk (SC-007): verify existing commands still work after adding statechart subcommand group.

---

## Work Package WP10: MSBuild Integration (Priority: P3)

**Goal**: Create an MSBuild targets file that automatically invokes `frank statechart generate --format all` after build, writing artifacts to `$(IntermediateOutputPath)statecharts/`.
**Independent Test**: Adding the target to a sample project and running `dotnet build` produces statechart spec files in the intermediate output directory.
**Prompt**: `/tasks/WP10-msbuild-integration.md`
**Estimated Size**: ~300 lines

### Included Subtasks
- [ ] T062 Create `src/Frank.Statecharts/build/Frank.Statecharts.targets` MSBuild targets file
- [ ] T063 Implement `GenerateStatechartSpecs` target: runs after `Build`, invokes `frank-cli statechart generate --format all --output $(IntermediateOutputPath)statecharts/`
- [ ] T064 Add condition to skip when no stateful resources present (graceful no-op)
- [ ] T065 Include targets file in `Frank.Statecharts.fsproj` as `buildTransitive` content
- [ ] T066 Verify target runs during `dotnet build` on a sample project with stateful resources
- [ ] T067 Verify target produces no errors on a project without stateful resources

### Implementation Notes
- MSBuild target: `AfterTargets="Build"` to run after compilation
- Output directory: `$(IntermediateOutputPath)statecharts/` (e.g., `obj/Debug/net10.0/statecharts/`)
- Target invokes `frank-cli` tool: requires frank-cli to be installed as a dotnet tool
- Idempotent: running twice produces identical output (SC-006)

### Parallel Opportunities
- None within this WP.

### Dependencies
- Depends on WP09 (CLI wiring must be complete for `frank-cli statechart generate` to work).

### Risks & Mitigations
- frank-cli must be installed as a dotnet tool for the MSBuild target to work. Document this prerequisite.
- Target should not fail the build if frank-cli is not installed (warning only).

---

## Dependency & Execution Summary

- **Sequence**: WP01 -> WP02 -> WP03, WP04 (parallel) -> WP05, WP06, WP07, WP08 (partially parallel) -> WP09 -> WP10
- **Parallelization**:
  - WP03 and WP04 can proceed in parallel after WP01+WP02
  - WP05 can start after WP02 (needs extractor)
  - WP06 can start after WP02+WP03 (needs extractor + pipeline)
  - WP07 can start after WP02+WP03+WP04 (needs all infrastructure)
  - WP08 can start after WP01+WP03+WP04 (no extractor needed, just parsers)
  - WP09 must wait for all commands (WP05-WP08)
  - WP10 must wait for WP09
- **MVP Scope**: WP01 + WP02 + WP03 + WP05 + WP06 + WP09 (extract + generate commands wired up)

---

## Subtask Index (Reference)

| Subtask ID | Summary | Work Package | Priority | Parallel? |
|------------|---------|--------------|----------|-----------|
| T001 | Add InternalsVisibleTo to Frank.Statecharts.fsproj | WP01 | P0 | No |
| T002 | Add Frank.Statecharts project reference to Frank.Cli.Core.fsproj | WP01 | P0 | No |
| T003 | Create XState Serializer.fs | WP01 | P0 | Yes |
| T004 | Create XState Deserializer.fs | WP01 | P0 | Yes |
| T005 | Add XState compile entries to fsproj | WP01 | P0 | No |
| T006 | Verify build | WP01 | P0 | No |
| T007 | Create StatechartExtractor.fs with ExtractedStatechart type | WP02 | P0 | No |
| T008 | Implement AssemblyLoadContext isolation | WP02 | P0 | No |
| T009 | Implement endpoint scanning for StateMachineMetadata | WP02 | P0 | No |
| T010 | Implement route template extraction | WP02 | P0 | No |
| T011 | Implement error handling for assembly loading | WP02 | P0 | No |
| T012 | Add StatechartExtractor.fs compile entry | WP02 | P0 | No |
| T013 | Verify module compiles | WP02 | P0 | No |
| T014 | Create FormatDetector.fs | WP03 | P1 | Yes |
| T015 | Create FormatPipeline.fs | WP03 | P1 | Yes |
| T016 | Implement format detection logic | WP03 | P1 | No |
| T017 | Implement generation pipelines | WP03 | P1 | No |
| T018 | Add compile entries to fsproj | WP03 | P1 | No |
| T019 | Verify modules compile | WP03 | P1 | No |
| T020 | Create StatechartDocumentJson.fs | WP04 | P1 | Yes |
| T021 | Create ValidationReportFormatter.fs | WP04 | P1 | Yes |
| T022 | Implement StatechartDocument JSON serialization | WP04 | P1 | No |
| T023 | Implement ValidationReport text/JSON formatting | WP04 | P1 | No |
| T024 | Add compile entries to fsproj | WP04 | P1 | No |
| T025 | Verify modules compile | WP04 | P1 | No |
| T026 | Create StatechartExtractCommand.fs | WP05 | P1 | No |
| T027 | Implement text output for extract | WP05 | P1 | No |
| T028 | Implement JSON output for extract | WP05 | P1 | No |
| T029 | Handle zero-resources case | WP05 | P1 | No |
| T030 | Handle assembly load errors | WP05 | P1 | No |
| T031 | Add compile entry to fsproj | WP05 | P1 | No |
| T032 | Create StatechartGenerateCommand.fs | WP06 | P1 | No |
| T033 | Implement single-format generation | WP06 | P1 | No |
| T034 | Implement --format all generation | WP06 | P1 | No |
| T035 | Implement --output file writing | WP06 | P1 | No |
| T036 | Implement --resource filter | WP06 | P1 | No |
| T037 | Implement stdout output with headers | WP06 | P1 | No |
| T038 | Handle zero-resources and directory creation | WP06 | P1 | No |
| T039 | Add compile entry to fsproj | WP06 | P1 | No |
| T040 | Create StatechartValidateCommand.fs | WP07 | P1 | No |
| T041 | Implement spec file parsing | WP07 | P1 | No |
| T042 | Implement code-truth artifact generation | WP07 | P1 | No |
| T043 | Implement validation orchestration | WP07 | P1 | No |
| T044 | Implement text output for validate | WP07 | P1 | No |
| T045 | Implement JSON output for validate | WP07 | P1 | No |
| T046 | Handle errors and exit codes | WP07 | P1 | No |
| T047 | Add compile entry to fsproj | WP07 | P1 | No |
| T048 | Create StatechartParseCommand.fs | WP08 | P2 | No |
| T049 | Implement format detection from extension | WP08 | P2 | No |
| T050 | Implement parser dispatch | WP08 | P2 | No |
| T051 | Implement ambiguous .json handling | WP08 | P2 | No |
| T052 | Implement JSON output of StatechartDocument | WP08 | P2 | No |
| T053 | Handle parse errors and exit codes | WP08 | P2 | No |
| T054 | Add compile entry to fsproj | WP08 | P2 | No |
| T055 | Add statechart parent Command to Program.fs | WP09 | P1 | No |
| T056 | Wire extract subcommand | WP09 | P1 | No |
| T057 | Wire generate subcommand | WP09 | P1 | No |
| T058 | Wire validate subcommand | WP09 | P1 | No |
| T059 | Wire parse subcommand | WP09 | P1 | No |
| T060 | Add help entries to HelpContent.fs | WP09 | P1 | Yes |
| T061 | Verify all commands accessible and no regressions | WP09 | P1 | No |
| T062 | Create Frank.Statecharts.targets MSBuild file | WP10 | P3 | No |
| T063 | Implement GenerateStatechartSpecs target | WP10 | P3 | No |
| T064 | Add condition for graceful no-op | WP10 | P3 | No |
| T065 | Include targets in fsproj as buildTransitive | WP10 | P3 | No |
| T066 | Verify target runs on sample project | WP10 | P3 | No |
| T067 | Verify no errors on project without stateful resources | WP10 | P3 | No |
