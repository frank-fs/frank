#!/bin/sh
# Entire CLI hooks
# Prepare-commit-msg hook: append Entire-Checkpoint trailer to the commit message.
# Skip silently on machines without Entire installed.
if command -v entire >/dev/null 2>&1; then
    entire hooks git prepare-commit-msg "$1" "$2" || true
fi
